<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Writers by Subject | Buy Argumentative Essay</title>
    <meta name="description" content="See the subject areas our argumentative essay writers cover, from business and nursing to literature and economics.">
    <meta name="robots" content="index,follow">
    <link rel="canonical" href="{{ url('/writers') }}">
    <link rel="icon" href="{{ asset('favicon.ico') }}" type="image/x-icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --blue: #1d7bff;
            --dark: #14141a;
            --muted: #5c5c6a;
            --bg: #f7f8fb;
            --card: #ffffff;
            --border: #e6e8f0;
            --accent: #f7ad26;
        }
        * { box-sizing: border-box; }
        body { margin: 0; font-family: 'Manrope', system-ui, -apple-system, sans-serif; background: var(--bg); color: var(--dark); line-height: 1.6; }
        a { color: inherit; text-decoration: none; }
        header { position: sticky; top: 0; z-index: 10; background: rgba(247,248,251,0.92); backdrop-filter: blur(12px); border-bottom: 1px solid rgba(230,232,240,0.6); }
        .nav { max-width: 1200px; margin: 0 auto; padding: 16px 24px; display: flex; align-items: center; justify-content: space-between; gap: 20px; }
        .brand { display: flex; align-items: center; gap: 10px; font-weight: 800; font-size: 20px; letter-spacing: -0.2px; white-space: nowrap; }
        .brand .mark { width: 38px; height: 38px; border-radius: 12px; background: linear-gradient(135deg, #1d7bff, #25c7ff); display: grid; place-items: center; color: #fff; font-size: 18px; font-weight: 800; flex: none; box-shadow: 0 12px 35px rgba(29,123,255,0.25); }
        .brand .text strong { color: var(--blue); }
        .menu { display: flex; align-items: center; gap: 22px; font-weight: 600; color: #202028; }
        .menu a { position: relative; padding: 6px 0; font-size: 15px; white-space: nowrap; }
        .menu a::after { content: ""; position: absolute; left: 0; bottom: -6px; height: 3px; width: 0; background: var(--blue); border-radius: 12px; transition: width .18s ease; }
        .menu a:hover::after, .menu a.active::after { width: 18px; }
        .actions { display: flex; align-items: center; gap: 10px; }
        .btn { border-radius: 10px; padding: 11px 16px; font-weight: 700; border: 1px solid transparent; transition: transform .15s ease, box-shadow .15s ease; font-size: 14px; white-space: nowrap; }
        .btn:hover { transform: translateY(-1px); }
        .btn-ghost { background: #ffffff; border-color: var(--border); color: var(--dark); }
        .btn-primary { background: linear-gradient(135deg, #1d7bff, #25c7ff); color: white; box-shadow: 0 12px 30px rgba(29,123,255,0.35); }
        main { max-width: 1200px; margin: 0 auto; padding: 40px 24px 60px; }
        h1 { margin: 0 0 8px; font-size: clamp(30px, 3vw + 8px, 44px); letter-spacing: -0.4px; }
        .lead { margin: 0 0 24px; color: var(--muted); font-weight: 600; max-width: 700px; }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 16px; }
        .card { background: var(--card); border: 1px solid var(--border); border-radius: 14px; padding: 20px; box-shadow: 0 15px 35px rgba(17,24,39,0.06); }
        .area { font-size: 18px; font-weight: 800; margin-bottom: 8px; }
        .topics { color: var(--muted); font-weight: 600; font-size: 14px; }
        .cta-panel { margin-top: 34px; background: linear-gradient(135deg, #1d7bff, #25c7ff); border-radius: 18px; padding: 30px; color: #fff; display: flex; align-items: center; justify-content: space-between; gap: 20px; flex-wrap: wrap; }
        .cta-panel h2 { margin: 0 0 6px; color: #fff; font-size: 24px; }
        .cta-panel p { margin: 0; opacity: 0.95; }
        .cta-panel .btn { background: #fff; color: #1d4ed8; }
        footer { background: #14141a; color: #c7c8d1; margin-top: 40px; }
        .footer-inner { max-width: 1200px; margin: 0 auto; padding: 50px 24px 34px; }
        .footer-grid { display: grid; grid-template-columns: 1.4fr repeat(3, 1fr); gap: 30px; }
        .footer-brand { font-size: 18px; font-weight: 800; color: #fff; display: flex; align-items: center; gap: 10px; }
        .footer-brand strong { color: #60a5fa; }
        .footer-desc { color: #9a9ba8; font-size: 14px; margin-top: 14px; max-width: 300px; line-height: 1.6; }
        .footer-col h4 { color: #fff; font-size: 14px; margin: 0 0 14px; letter-spacing: 0.4px; text-transform: uppercase; }
        .footer-col a { display: block; color: #c7c8d1; font-size: 14px; padding: 5px 0; }
        .footer-col a:hover { color: #60a5fa; }
        .footer-bottom { border-top: 1px solid rgba(255,255,255,0.08); margin-top: 34px; padding-top: 20px; display: flex; justify-content: space-between; gap: 16px; flex-wrap: wrap; font-size: 13px; color: #8b8c98; }
        @media (max-width: 900px) { header { position: static; } .menu { display: none; } .footer-grid { grid-template-columns: 1fr 1fr; } }
        @media (max-width: 560px) { .footer-grid { grid-template-columns: 1fr; } .actions .btn-ghost { display: none; } }
    </style>
</head>
<body>
    <header>
        <div class="nav">
            <a class="brand" href="{{ url('/') }}">
                <span class="mark">✍</span>
                <span class="text">Buy <strong>Argumentative Essay</strong></span>
            </a>
            <nav class="menu" aria-label="Primary">
                <a href="{{ url('/') }}">Home</a>
                <a href="{{ url('/argumentative-essay-writing-service') }}">Writing Service</a>
                <a href="{{ url('/how-it-works') }}">How It Works</a>
                <a href="{{ url('/pricing') }}">Pricing</a>
                <a href="{{ url('/writers') }}" class="active">Writers</a>
            </nav>
            <div class="actions">
                <a class="btn btn-ghost" href="{{ route('order', ['tab' => 'existing']) }}">Sign In</a>
                <a class="btn btn-primary" href="{{ route('order', ['tab' => 'new']) }}">Order Now</a>
            </div>
        </div>
    </header>

    <main>
        <h1>Our Writers</h1>
        <p class="lead">Your essay is matched to a writer with knowledge of your subject area, from business and nursing to literature and economics.</p>

        <section class="grid">
            @foreach($writers as $writer)
                <article class="card">
                    <div class="area">{{ $writer['area'] }}</div>
                    <div class="topics">{{ $writer['topics'] }}</div>
                </article>
            @endforeach
        </section>

        <section class="cta-panel">
            <div>
                <h2>Need a writer for your essay?</h2>
                <p>Share your subject and instructions, and we will match your order to a suitable writer.</p>
            </div>
            <a class="btn" href="{{ route('order', ['tab' => 'new']) }}">Order Now</a>
        </section>
    </main>

    <footer>
        <div class="footer-inner">
            <div class="footer-grid">
                <div>
                    <div class="footer-brand"><span>✍</span><span>Buy <strong>Argumentative Essay</strong></span></div>
                    <p class="footer-desc">Custom argumentative essay writing support, research and editing help for students at every academic level.</p>
                </div>
                <div class="footer-col">
                    <h4>Services</h4>
                    <a href="{{ url('/argumentative-essay-writing-service') }}">Argumentative Essay Writing Service</a>
                    <a href="{{ url('/argumentative-essay-writer') }}">Argumentative Essay Writer</a>
                    <a href="{{ url('/write-my-argumentative-essay') }}">Write My Argumentative Essay</a>
                    <a href="{{ url('/argumentative-essay-help') }}">Argumentative Essay Help</a>
                    <a href="{{ url('/argumentative-essay-editing') }}">Argumentative Essay Editing</a>
                    <a href="{{ url('/argumentative-essay-proofreading') }}">Argumentative Essay Proofreading</a>
                </div>
                <div class="footer-col">
                    <h4>Company</h4>
                    <a href="{{ url('/about-us') }}">About Us</a>
                    <a href="{{ url('/how-it-works') }}">How It Works</a>
                    <a href="{{ url('/pricing') }}">Pricing</a>
                    <a href="{{ url('/reviews') }}">Reviews</a>
                    <a href="{{ url('/guarantees') }}">Guarantees</a>
                    <a href="{{ url('/contact-us') }}">Contact Us</a>
                </div>
                <div class="footer-col">
                    <h4>Policies</h4>
                    <a href="{{ url('/revision-policy') }}">Revision Policy</a>
                    <a href="{{ url('/refund-policy') }}">Refund Policy</a>
                    <a href="{{ url('/privacy-policy') }}">Privacy Policy</a>
                    <a href="{{ url('/terms-and-conditions') }}">Terms &amp; Conditions</a>
                    <a href="{{ url('/academic-integrity') }}">Academic Integrity</a>
                </div>
            </div>
            <div class="footer-bottom">
                <span>&copy; {{ date('Y') }} Buy Argumentative Essay. All rights reserved.</span>
                <span>Model papers and writing support for research and reference.</span>
            </div>
        </div>
    </footer>
</body>
</html>
